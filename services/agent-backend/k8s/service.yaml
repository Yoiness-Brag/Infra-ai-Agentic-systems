apiVersion: v1
kind: Service
metadata:
  name: agent-backend
  namespace: ai-platform
  labels:
    app.kubernetes.io/name: agent-backend
    app.kubernetes.io/part-of: infra-ai-mvp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-backend
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
